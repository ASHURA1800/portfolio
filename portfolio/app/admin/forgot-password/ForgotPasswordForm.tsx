// Phase 5.3 — delegates to the RecoveryForm component.
// Kept as a named export so any existing import of ForgotPasswordForm doesn't break.
export { RecoveryForm as ForgotPasswordForm } from '@/components/admin/auth/recovery';
