// app/admin/(protected)/skills/page.tsx — NO CHANGES REQUIRED
//
// The existing page.tsx already does exactly what Phase 6.4 needs:
//   1. Fetches skills from DB ordered by order_index (force-dynamic)
//   2. Passes them as `initial` to SkillsManager
//
// Only SkillsManager.tsx is replaced (in this deliverable).
// The page.tsx from the original codebase works unchanged.
