// Delegates to the ResetForm component.
// Kept as a named export so any existing import of ResetPasswordForm doesn't break.
export { ResetForm as ResetPasswordForm } from '@/components/admin/auth/recovery'; 
